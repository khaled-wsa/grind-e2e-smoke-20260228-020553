# This project currently boots without loading environment variables.
# Use this file as a template if your deployment introduces env-based config.

APP_ENV=production
APP_DEBUG=0
APP_URL=https://example.com
SESSION_COOKIE_SECURE=1

# Optional deployment override if you externalize SQLite storage.
SQLITE_DB_FILE=/var/lib/grind-e2e-smoke/app.sqlite
